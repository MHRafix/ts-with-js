// normal veriable type
let a: string;

// union type
let b: string | number | boolean;

// union type array
let c: (number | string | boolean | object)[];

// any type
let d: any;

// object type
let obj: {
	name: string;
	age: number;
	isAdult: boolean;
};

// array type
let array = [];
